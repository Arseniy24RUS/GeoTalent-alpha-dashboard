(async()=>{const read=async names=>(await Promise.all(names.map(n=>fetch(`./assets/${n}`).then(r=>{if(!r.ok)throw new Error(`${n}: ${r.status}`);return r.text()})))).join('');const seq=(prefix,count,ext='txt')=>Array.from({length:count},(_,i)=>`${prefix}-${String(i).padStart(2,'0')}.${ext}`);try{const[data,hero,world,app]=await Promise.all([read(seq('data',5)),read(seq('hero',5)),read(seq('world',3)),read(seq('app',2))]);new Function(data)();window.GEOTALENT_HERO_B64=hero;window.GEOTALENT_WORLD_SVG=world;new Function(app)()}catch(error){console.error(error);document.body.insertAdjacentHTML('afterbegin',`<div style="padding:14px;background:#fff3da;color:#0a132d;font:14px Arial">Не удалось загрузить аналитические модули: ${String(error.message||error)}</div>`)}})();